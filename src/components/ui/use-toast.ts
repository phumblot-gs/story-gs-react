
import { useToast, toast } from "@/hooks/use-toast";

/**
 * Re-export des méthodes useToast et toast depuis hooks/use-toast
 * pour assurer la cohérence de l'API à travers l'application
 */
export { useToast, toast };
